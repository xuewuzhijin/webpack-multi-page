body {
  background-color: $color;
  display: grid;
  grid-template-columns: 1fr 1fr;
  grid-template-rows: auto auto;
  grid-gap: 20px;
}

body div {
  background-image: url("~@views/index/img.jpg")
}